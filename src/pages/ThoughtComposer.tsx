import { useState } from 'react';
import { Modal } from '../components/Modal';
export function ThoughtComposer({open,onClose}:{open:boolean;onClose:()=>void}){const [kind,setKind]=useState('Thought'); const [text,setText]=useState(''); return <Modal open={open} onClose={onClose} title="What just happened?"><div className="composer-meta">Rebecca · Chapter 12</div><div className="composer-tabs">{['Thought','Quote','Question','Prediction'].map(k=><button key={k} className={kind===k?'active':''} onClick={()=>setKind(k)}>{k}</button>)}</div><textarea value={text} onChange={e=>setText(e.target.value)} placeholder={kind==='Prediction'?'I think…':'Say it while you remember it…'}/><div className="composer-options"><label><input type="checkbox"/> Mark as spoiler</label><span>Visible through Ch. 12</span></div><button className="primary" onClick={()=>{setText('');onClose()}}>{kind==='Prediction'?'Lock prediction':'Post'}</button></Modal>}
