import {createContext,useContext,useEffect,useState} from 'react';
type Router={path:string;navigate:(to:string,replace?:boolean)=>void};const C=createContext<Router|null>(null);
export function RouterProvider({children}:{children:React.ReactNode}){const[path,setPath]=useState(location.pathname);useEffect(()=>{const f=()=>{setPath(location.pathname);window.scrollTo({top:0,left:0,behavior:'auto'})};addEventListener('popstate',f);return()=>removeEventListener('popstate',f)},[]);const navigate=(to:string,replace=false)=>{if(replace)history.replaceState({},'',to);else history.pushState({},'',to);setPath(to);window.scrollTo({top:0,left:0,behavior:'auto'})};return <C.Provider value={{path,navigate}}>{children}</C.Provider>}
export const useRouter=()=>{const x=useContext(C);if(!x)throw new Error('Missing router');return x};
export function match(path:string,pattern:string){const keys:string[]=[];const re=new RegExp('^'+pattern.replace(/:[^/]+/g,(m)=>{keys.push(m.slice(1));return'([^/]+)'})+'/?$');const m=path.match(re);if(!m)return null;return Object.fromEntries(keys.map((k,i)=>[k,decodeURIComponent(m[i+1])])) as Record<string,string>}
