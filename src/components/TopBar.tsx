import { Bell, ChevronDown } from 'lucide-react';
import { members, type Club } from '../data/demo';
export function TopBar({club,onClubSwitch,onNotifications,onProfile}:{club:Club;onClubSwitch?:()=>void;onNotifications?:()=>void;onProfile?:()=>void}){return <header className="topbar"><button className="club-wordmark" onClick={onClubSwitch} aria-label="Switch club"><span className={`club-mark ${club.tone}`}>{club.mark}</span><span>{club.name}</span><ChevronDown size={16}/></button><div className="top-actions"><button className="icon-btn" aria-label="Notifications" onClick={onNotifications}><Bell size={19}/><span className="notification-dot"/></button><button className="avatar-button" onClick={onProfile} aria-label="Open your profile"><img className="avatar" src={members[0].avatar} alt="Neha"/></button></div></header>}
